# json-stream-compressor
Simple compressor, which takes as input a text stream (each line is flat JSON object), and outputs its data in TLV; JSON object keys are substituted with numbers, to save space.
